# 🎨 登录/注册页面 - 高保真设计稿

**页面：** 登录/注册页  
**版本：** v1.0  
**日期：** 2026-03-28  
**设计师：** 小雅  
**状态：** ✅ 完成

---

## 一、页面概述

### 1.1 页面信息

| 属性 | 值 |
|------|-----|
| 页面尺寸 | 响应式（PC: 1920x1080, 平板：768x1024, 手机：375x667） |
| 背景色 | `linear-gradient(135deg, #667eea 0%, #764ba2 100%)` |
| 主色调 | 紫色渐变（温暖、治愈） |
| 核心功能 | 用户登录、用户注册、密码找回 |

### 1.2 用户流程

```
┌─────────────────────────────────────────────────────────────┐
│                     登录/注册页面                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  未注册用户                          已注册用户              │
│       ↓                                   ↓                │
│  点击"注册"Tab                      点击"登录"Tab           │
│       ↓                                   ↓                │
│  输入手机号/邮箱                   输入账号 + 密码          │
│  设置密码                               ↓                │
│  确认密码                          点击"登录"按钮           │
│       ↓                                   ↓                │
│  点击"注册"按钮                    验证成功                  │
│       ↓                                   ↓                │
│  验证成功                          跳转首页                  │
│       ↓                                                    │
│  自动登录                                                 │
│       ↓                                                    │
│  跳转首页                                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、页面布局

### 2.1 整体结构

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                    【渐变背景层】                           │
│                                                            │
│         ┌──────────────────────────────────┐              │
│         │                                  │              │
│         │      ┌────────────────────┐     │              │
│         │      │   🎨 心情打卡       │     │              │
│         │      │   Mood Checker      │     │              │
│         │      └────────────────────┘     │              │
│         │                                  │              │
│         │  ┌──────────────────────────┐   │              │
│         │  │                          │   │              │
│         │  │   [登录]  [注册]         │   │              │
│         │  │                          │   │              │
│         │  │   ┌────────────────┐    │   │              │
│         │  │   │ 账号输入框      │    │   │              │
│         │  │   └────────────────┘    │   │              │
│         │  │                          │   │              │
│         │  │   ┌────────────────┐    │   │              │
│         │  │   │ 密码输入框      │    │   │              │
│         │  │   └────────────────┘    │   │              │
│         │  │                          │   │              │
│         │  │   ┌────────────────┐    │   │              │
│         │  │   │   登录按钮      │    │   │              │
│         │  │   └────────────────┘    │   │              │
│         │  │                          │   │              │
│         │  │   忘记密码？| 立即注册   │   │              │
│         │  │                          │   │              │
│         │  └──────────────────────────┘   │              │
│         │                                  │              │
│         └──────────────────────────────────┘              │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 2.2 响应式布局

#### PC 端（≥1024px）
- 卡片宽度：`440px`
- 卡片高度：`auto`（最小 `520px`）
- 卡片居中：垂直 + 水平居中
- 内边距：`48px`

#### 平板端（768px - 1023px）
- 卡片宽度：`400px`
- 卡片高度：`auto`
- 卡片居中：垂直 + 水平居中
- 内边距：`40px`

#### 手机端（< 768px）
- 卡片宽度：`100% - 32px`（左右各留 16px）
- 卡片高度：`auto`
- 卡片位置：垂直居中偏上
- 内边距：`32px 24px`

---

## 三、详细设计规格

### 3.1 背景层

```css
/* 渐变背景 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 装饰元素 - 漂浮的圆形 */
装饰圆 1: 
  - 位置：左上角 (10%, 15%)
  - 尺寸：300px × 300px
  - 颜色：rgba(255, 255, 255, 0.1)
  - 模糊：blur(60px)
  
装饰圆 2:
  - 位置：右下角 (85%, 80%)
  - 尺寸：400px × 400px
  - 颜色：rgba(255, 255, 255, 0.08)
  - 模糊：blur(80px)
```

### 3.2 Logo 区域

```
┌─────────────────────────────┐
│                             │
│        🎨 心情打卡          │
│        Mood Checker         │
│                             │
└─────────────────────────────┘
```

**规格：**
- Logo 图标：`🎨` (emoji) 或 SVG 图标
- 图标尺寸：`48px × 48px`
- 主标题："心情打卡"
  - 字号：`28px`
  - 字重：`700 (bold)`
  - 颜色：`#FFFFFF`
  - 字间距：`2px`
- 副标题："Mood Checker"
  - 字号：`14px`
  - 字重：`400`
  - 颜色：`rgba(255, 255, 255, 0.8)`
  - 字间距：`1px`
- 间距：图标与主标题 `16px`，主标题与副标题 `8px`
- 底部间距：`40px`

### 3.3 Tab 切换器

```
┌─────────────────────────────────────┐
│  ┌──────────┐  ┌──────────┐        │
│  │   登录   │  │   注册   │        │
│  │  (激活)  │  │          │        │
│  └──────────┘  └──────────┘        │
│  ───────────────────────────────    │
└─────────────────────────────────────┘
```

**规格：**
- 容器背景：`rgba(255, 255, 255, 0.1)`
- 容器圆角：`rounded-xl (12px)`
- 容器内边距：`4px`
- Tab 按钮：
  - 宽度：`120px`
  - 高度：`44px`
  - 激活态背景：`#FFFFFF`
  - 激活态文字：`#667eea`
  - 非激活态背景：`transparent`
  - 非激活态文字：`rgba(255, 255, 255, 0.7)`
  - 字号：`16px`
  - 字重：`600 (semibold)`
  - 圆角：`rounded-lg (8px)`
  - 过渡：`all 0.3s ease`
- 底部间距：`32px`

### 3.4 表单输入框

#### 账号输入框（登录页）

```
┌─────────────────────────────────────┐
│  👤  手机号/邮箱                     │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 容器高度：`52px`
- 背景：`#FFFFFF`
- 边框：`1px solid #E5E7EB`
- 圆角：`rounded-lg (8px)`
- 内边距：`0 16px`
- 图标：`👤` 或 Lucide User 图标
  - 尺寸：`20px`
  - 颜色：`#9CA3AF`
  - 右边距：`12px`
- 输入文字：
  - 字号：`16px`
  - 颜色：`#1F2937`
  - 占位符颜色：`#9CA3AF`
- 焦点状态：
  - 边框：`2px solid #667eea`
  - 阴影：`0 0 0 3px rgba(102, 126, 234, 0.1)`
- 错误状态：
  - 边框：`2px solid #EF4444`
  - 错误文字：`#EF4444`，字号 `14px`，位于输入框下方
- 底部间距：`20px`

#### 密码输入框

```
┌─────────────────────────────────────┐
│  🔒  密码                      👁️   │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 与账号输入框相同
- 右侧添加"显示/隐藏密码"切换按钮
  - 图标：`👁️` 或 Lucide Eye/EyeOff
  - 尺寸：`20px`
  - 颜色：`#9CA3AF`
  - 可点击区域：`44px × 44px`
- 底部间距：`8px`

#### 确认密码输入框（注册页）

```
┌─────────────────────────────────────┐
│  🔒  确认密码                  👁️   │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 与密码输入框相同
- 底部间距：`8px`

#### 用户名输入框（注册页）

```
┌─────────────────────────────────────┐
│  👤  用户名                         │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 与账号输入框相同
- 底部间距：`20px`

### 3.5 提交按钮

```
┌─────────────────────────────────────┐
│                                     │
│           登  录                    │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 宽度：`100%`
- 高度：`52px`
- 背景：`linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- 文字：
  - 内容："登录" 或 "注册"
  - 字号：`18px`
  - 字重：`600 (semibold)`
  - 颜色：`#FFFFFF`
  - 字间距：`4px`
- 圆角：`rounded-lg (8px)`
- 阴影：`shadow-md`
- 悬停状态：
  - 背景：`linear-gradient(135deg, #5568d3 0%, #63408a 100%)`
  - 阴影：`shadow-lg`
  - 位移：`transform: translateY(-2px)`
- 点击状态：
  - 位移：`transform: translateY(0)`
  - 阴影：`shadow-sm`
- 禁用状态：
  - 背景：`#D1D5DB`
  - 文字：`#9CA3AF`
  - 阴影：`none`
  - 光标：`not-allowed`
- 加载状态：
  - 显示加载动画（旋转圆圈）
  - 文字隐藏
- 底部间距：`24px`

### 3.6 辅助链接

```
┌─────────────────────────────────────┐
│                                     │
│   忘记密码？      没有账号？注册    │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 布局：Flexbox，两端对齐
- 链接文字：
  - 字号：`14px`
  - 字重：`500 (medium)`
  - 颜色：`rgba(255, 255, 255, 0.8)`
  - 悬停颜色：`#FFFFFF`
  - 下划线：悬停时显示
- 间距：两个链接之间 `auto`

---

## 四、交互状态

### 4.1 输入框验证

#### 实时验证（onBlur）

| 场景 | 触发时机 | 验证规则 | 错误提示 |
|------|----------|----------|----------|
| 手机号 | 失去焦点 | 11 位数字，1 开头 | "请输入正确的手机号" |
| 邮箱 | 失去焦点 | 标准邮箱格式 | "请输入正确的邮箱地址" |
| 密码 | 失去焦点 | 6-20 位，含字母和数字 | "密码需 6-20 位，包含字母和数字" |
| 用户名 | 失去焦点 | 2-20 位字符 | "用户名需 2-20 位字符" |
| 确认密码 | 失去焦点 | 与密码一致 | "两次输入的密码不一致" |

#### 错误状态样式

```css
.input-error {
  border-color: #EF4444;
  border-width: 2px;
}

.error-message {
  color: #EF4444;
  font-size: 14px;
  margin-top: 8px;
  display: flex;
  align-items: center;
  gap: 6px;
}

.error-message::before {
  content: "⚠️";
  font-size: 14px;
}
```

### 4.2 按钮状态

```
正常状态 → 悬停状态 → 点击状态 → 加载状态 → 成功状态
   ↓           ↓           ↓           ↓           ↓
 默认样式   上移 2px    回弹      显示 loading  显示✓图标
           阴影加深    阴影减小   禁用点击     绿色背景
```

### 4.3 Tab 切换动画

```css
.tab-transition {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.tab-active {
  background: #FFFFFF;
  color: #667eea;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.tab-inactive {
  background: transparent;
  color: rgba(255, 255, 255, 0.7);
}

.tab-inactive:hover {
  color: rgba(255, 255, 255, 0.9);
  background: rgba(255, 255, 255, 0.05);
}
```

---

## 五、成功/失败反馈

### 5.1 登录/注册成功

```
┌─────────────────────────────────────┐
│                                     │
│           ✅                        │
│                                     │
│       登录成功！                    │
│       正在跳转首页...               │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 弹窗背景：`rgba(255, 255, 255, 0.95)`
- 图标：`✅` 或自定义成功图标
  - 尺寸：`64px`
  - 动画：缩放弹跳
- 标题：
  - 文字："登录成功！"
  - 字号：`20px`
  - 字重：`600`
  - 颜色：`#10B981`
- 副标题：
  - 文字："正在跳转首页..."
  - 字号：`14px`
  - 颜色：`#6B7280`
- 持续时间：`2 秒` 后自动关闭并跳转

### 5.2 登录/注册失败

```
┌─────────────────────────────────────┐
│                                     │
│           ❌                        │
│                                     │
│       登录失败                      │
│       账号或密码错误，请重试        │
│                                     │
│       ┌──────────────┐             │
│       │    确 定      │             │
│       └──────────────┘             │
│                                     │
└─────────────────────────────────────┘
```

**规格：**
- 弹窗背景：`rgba(255, 255, 255, 0.95)`
- 图标：`❌` 或自定义错误图标
  - 尺寸：`64px`
  - 动画：左右摇晃
- 标题：
  - 文字："登录失败"
  - 字号：`20px`
  - 字重：`600`
  - 颜色：`#EF4444`
- 副标题：
  - 文字：具体错误信息
  - 字号：`14px`
  - 颜色：`#6B7280`
- 按钮：
  - 文字："确定"
  - 样式：与主按钮相同
  - 点击关闭弹窗

---

## 六、Tailwind CSS 实现代码

### 6.1 完整组件代码

```tsx
// components/auth/AuthPage.tsx
import { useState } from 'react'
import { Eye, EyeOff, User, Lock, Mail, Phone } from 'lucide-react'

export default function AuthPage() {
  const [isLogin, setIsLogin] = useState(true)
  const [showPassword, setShowPassword] = useState(false)
  const [formData, setFormData] = useState({
    account: '',
    password: '',
    confirmPassword: '',
    username: ''
  })
  const [errors, setErrors] = useState<Record<string, string>>({})
  const [isLoading, setIsLoading] = useState(false)

  return (
    <div className="min-h-screen w-full relative overflow-hidden">
      {/* 背景渐变 */}
      <div className="absolute inset-0 bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500" />
      
      {/* 装饰圆形 */}
      <div className="absolute top-[15%] left-[10%] w-[300px] h-[300px] bg-white/10 rounded-full blur-3xl" />
      <div className="absolute bottom-[20%] right-[15%] w-[400px] h-[400px] bg-white/8 rounded-full blur-3xl" />
      
      {/* 主容器 */}
      <div className="relative min-h-screen flex items-center justify-center p-4">
        {/* 卡片 */}
        <div className="w-full max-w-md bg-white/95 backdrop-blur-xl rounded-2xl shadow-2xl p-8 md:p-12">
          {/* Logo */}
          <div className="text-center mb-10">
            <div className="text-5xl mb-4">🎨</div>
            <h1 className="text-2xl font-bold text-white mb-2">心情打卡</h1>
            <p className="text-sm text-white/80 tracking-wide">Mood Checker</p>
          </div>
          
          {/* Tab 切换 */}
          <div className="flex bg-white/10 rounded-xl p-1 mb-8">
            <button
              onClick={() => setIsLogin(true)}
              className={`flex-1 py-3 px-4 rounded-lg text-base font-semibold transition-all duration-300 ${
                isLogin 
                  ? 'bg-white text-indigo-600 shadow-md' 
                  : 'text-white/70 hover:text-white hover:bg-white/5'
              }`}
            >
              登录
            </button>
            <button
              onClick={() => setIsLogin(false)}
              className={`flex-1 py-3 px-4 rounded-lg text-base font-semibold transition-all duration-300 ${
                !isLogin 
                  ? 'bg-white text-indigo-600 shadow-md' 
                  : 'text-white/70 hover:text-white hover:bg-white/5'
              }`}
            >
              注册
            </button>
          </div>
          
          {/* 表单 */}
          <form className="space-y-5">
            {/* 注册 - 用户名 */}
            {!isLogin && (
              <div className="space-y-1">
                <div className="relative">
                  <User className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" />
                  <input
                    type="text"
                    placeholder="用户名"
                    className="w-full h-[52px] pl-12 pr-4 bg-white border border-gray-200 rounded-lg text-base focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 transition-all outline-none"
                  />
                </div>
              </div>
            )}
            
            {/* 账号输入 */}
            <div className="space-y-1">
              <div className="relative">
                <Mail className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" />
                <input
                  type="text"
                  placeholder="手机号/邮箱"
                  className="w-full h-[52px] pl-12 pr-4 bg-white border border-gray-200 rounded-lg text-base focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 transition-all outline-none"
                />
              </div>
              {errors.account && (
                <p className="text-red-500 text-sm flex items-center gap-1.5">
                  <span>⚠️</span> {errors.account}
                </p>
              )}
            </div>
            
            {/* 密码输入 */}
            <div className="space-y-1">
              <div className="relative">
                <Lock className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" />
                <input
                  type={showPassword ? 'text' : 'password'}
                  placeholder="密码"
                  className="w-full h-[52px] pl-12 pr-12 bg-white border border-gray-200 rounded-lg text-base focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 transition-all outline-none"
                />
                <button
                  type="button"
                  onClick={() => setShowPassword(!showPassword)}
                  className="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600 transition-colors"
                >
                  {showPassword ? <EyeOff className="w-5 h-5" /> : <Eye className="w-5 h-5" />}
                </button>
              </div>
              {errors.password && (
                <p className="text-red-500 text-sm flex items-center gap-1.5">
                  <span>⚠️</span> {errors.password}
                </p>
              )}
            </div>
            
            {/* 注册 - 确认密码 */}
            {!isLogin && (
              <div className="space-y-1">
                <div className="relative">
                  <Lock className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" />
                  <input
                    type={showPassword ? 'text' : 'password'}
                    placeholder="确认密码"
                    className="w-full h-[52px] pl-12 pr-12 bg-white border border-gray-200 rounded-lg text-base focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 transition-all outline-none"
                  />
                  <button
                    type="button"
                    onClick={() => setShowPassword(!showPassword)}
                    className="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600 transition-colors"
                  >
                    {showPassword ? <EyeOff className="w-5 h-5" /> : <Eye className="w-5 h-5" />}
                  </button>
                </div>
                {errors.confirmPassword && (
                  <p className="text-red-500 text-sm flex items-center gap-1.5">
                    <span>⚠️</span> {errors.confirmPassword}
                  </p>
                )}
              </div>
            )}
            
            {/* 提交按钮 */}
            <button
              type="submit"
              disabled={isLoading}
              className="w-full h-[52px] bg-gradient-to-r from-indigo-500 to-purple-600 text-white text-lg font-semibold rounded-lg shadow-md hover:shadow-lg hover:-translate-y-0.5 active:translate-y-0 active:shadow-sm transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:translate-y-0 disabled:hover:shadow-md"
            >
              {isLoading ? (
                <div className="w-6 h-6 border-2 border-white border-t-transparent rounded-full animate-spin mx-auto" />
              ) : (
                isLogin ? '登录' : '注册'
              )}
            </button>
            
            {/* 辅助链接 */}
            {isLogin && (
              <div className="flex justify-between items-center">
                <a href="/forgot-password" className="text-sm text-white/80 hover:text-white transition-colors">
                  忘记密码？
                </a>
                <button
                  type="button"
                  onClick={() => setIsLogin(false)}
                  className="text-sm text-white/80 hover:text-white transition-colors"
                >
                  没有账号？<span className="font-medium">注册</span>
                </button>
              </div>
            )}
            
            {!isLogin && (
              <div className="text-center">
                <button
                  type="button"
                  onClick={() => setIsLogin(true)}
                  className="text-sm text-white/80 hover:text-white transition-colors"
                >
                  已有账号？<span className="font-medium">登录</span>
                </button>
              </div>
            )}
          </form>
        </div>
      </div>
    </div>
  )
}
```

---

## 七、设计资源

### 7.1 图标资源

| 图标 | 来源 | 尺寸 |
|------|------|------|
| 🎨 Logo | Emoji 或自定义 SVG | 48px |
| 👤 用户 | Lucide User | 20px |
| 🔒 密码 | Lucide Lock | 20px |
| 👁️ 显示密码 | Lucide Eye/EyeOff | 20px |
| ✉️ 邮箱 | Lucide Mail | 20px |
| 📱 手机 | Lucide Phone | 20px |

### 7.2 动画资源

- Loading 动画：CSS 旋转圆圈
- 成功动画：缩放弹跳
- 失败动画：左右摇晃
- Tab 切换：透明度 + 位移动画

---

## 八、验收标准

### 8.1 视觉验收

- [ ] 背景渐变与设计方案一致
- [ ] 卡片圆角、阴影符合规范
- [ ] 字体大小、字重、颜色正确
- [ ] 输入框、按钮尺寸准确
- [ ] 间距符合 4px 网格系统

### 8.2 交互验收

- [ ] Tab 切换流畅，动画自然
- [ ] 输入框焦点状态明显
- [ ] 密码显示/隐藏功能正常
- [ ] 按钮悬停、点击效果正确
- [ ] 表单验证提示准确
- [ ] 加载状态显示正确
- [ ] 成功/失败反馈清晰

### 8.3 响应式验收

- [ ] PC 端（1920x1080）显示正常
- [ ] 平板端（768x1024）显示正常
- [ ] 手机端（375x667）显示正常
- [ ] 触摸区域≥44px

---

**设计师：** 小雅  
**审核人：** 大伟  
**前端对接：** 小林  
**最后更新：** 2026-03-28
