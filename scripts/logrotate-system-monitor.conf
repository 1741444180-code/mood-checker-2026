/var/log/system-monitor.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 644 root root
    postrotate
        # Optional: reload or restart services if needed
        # systemctl reload rsyslog > /dev/null 2>&1 || true
    endscript
}